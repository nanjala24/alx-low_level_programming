#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main -entry point
 * Author: nanjala24
 * Return 0 (success)
 */
int main(void)
{
	int numbers;

	for (numbers = 30; numbers < 40; numbers++)
		putchar(numbers);
	putchar('\n');
	return (0)

}
